# Mosquitto 브로커 설정 - Situation Pro MQTT
# allow_anonymous 은 listener 정의보다 반드시 앞에 위치해야 함 (Mosquitto 2.x 규칙)

allow_anonymous true

# 백엔드(aiomqtt) 전용 TCP 포트 - 기존 Windows 서비스(1883)와 충돌 방지
listener 1885

# 프론트엔드(브라우저 mqtt.js) WebSocket 포트
listener 9001
protocol websockets
